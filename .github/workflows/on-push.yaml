name: on-push
run-name: On push to main

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  actions: read
  contents: write
  pull-requests: write

jobs:
  build-site:
    uses: ./.github/workflows/build-site.yaml
