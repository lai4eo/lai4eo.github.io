name: on-schedule
run-name: On schedule

on:
  # run periodically
  schedule:
    - cron: "0 0 * * 1"

  # run manually by user
  workflow_dispatch:

permissions:
  actions: read
  contents: write
  pull-requests: write

jobs:
  build-site:
    uses: ./.github/workflows/build-site.yaml
